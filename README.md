This project is a Python program that demonstrates how to work with dictionaries using countries and their capitals. It checks for the existence of specific keys, retrieves values safely using the .get() method, and updates the dictionary by adding and modifying entries. The program also shows how to remove items, iterate through keys and values, and includes a mini interactive feature where users can search for a country and optionally add it if it doesn’t exist.
